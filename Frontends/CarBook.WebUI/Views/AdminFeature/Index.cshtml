@model List<ResultFeatureDto>
@{
	ViewData["Title"] = "AdminCar";
	Layout = "~/Views/AdminLayout/Index.cshtml";
	int count = 0;
}

<div class="page-content">
	<div class="container-fluid">
		<div class="row">
			<div class="col-xl-12">
				<div class="card">
					<div class="card-body">
						<h4 class="card-title">Car Features List</h4>
						<p class="card-subtitle mb-4">
							You can add new features.
						</p>

						<div class="table-responsive">
							<table class="table mb-0">
								<thead>
									<tr>
										<th>#</th>
										<th>Feature</th>
										<th>Delete</th>
										<th>Update</th>
									</tr>
								</thead>
								<tbody>

									@foreach (var item in Model)
									{
										count++;

										<tr>
											<th scope="row">@count</th>
											<td>@item.Name</td>
											<td><a asp-asp-controller="AdminFeature" asp-action="RemoveFeature" asp-route-id="@item.FeatureID" class="btn btn-outline-danger">Delete</a></td>

											<td><a href="/AdminFeature/UpdateFeature/@item.FeatureID" class="btn btn-outline-primary">Update</a></td>

										</tr>
									}

								</tbody>
							</table>
							<a href="/AdminFeature/CreateFeature" class="btn btn-outline-success">Create a New Feature</a>

						</div>

					</div>
					<!-- end card-body-->
				</div>
				<!-- end card -->

			</div>
			<!-- end col -->
			<!-- end col -->
		</div>

	</div> <!-- container-fluid -->
</div>

